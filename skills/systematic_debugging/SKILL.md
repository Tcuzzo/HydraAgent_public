---
name: systematic-debugging
description: Root-cause bugs, failures, and unexpected behavior before proposing fixes. Map the real error path, verify live state, and fix the root cause — never patch symptoms.
license: MIT
version: "1.0"
allowed-tools:
  - fs_read
  - list_directory
  - grep
  - shell
---
# Systematic Debugging

Trace every joined mechanism end-to-end before proposing a fix. Verify against live code and runtime state. Fix root cause, never symptoms.
